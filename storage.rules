rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow read access to everyone
      allow read: if true;
      
      // Allow write access to authenticated users
      // In production, you would restrict this to admin roles
      allow write: if request.auth != null;
    }
  }
}